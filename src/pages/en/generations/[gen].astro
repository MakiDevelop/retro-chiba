---
import GenerationPage from '../../../components/GenerationPage.astro';
import { allGenKeys } from '../../../data/generations';

export function getStaticPaths() {
  return allGenKeys.map((gen) => ({ params: { gen }, props: { gen } }));
}

const { gen } = Astro.props;
---

<GenerationPage gen={gen} lang="en" />
